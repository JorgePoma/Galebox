import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.galebox.app',
  appName: 'GaleboxAPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
